import Button from "../../ui/Button";

function CompleteButton({ bookingId }) {
  return (
    <Button variation="primary" size="small">
      Complete
    </Button>
  );
}

export default CompleteButton;
